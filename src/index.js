import React from "react"
import ReactDOM from "react-dom"
import "./styles"

// Render app
const App = () => (
  <div>
		<h1>Hello, Parcel.</h1>
  </div>
)
ReactDOM.render( 
  <App />,
  document.getElementById("root") 
)